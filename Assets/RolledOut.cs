using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RolledOut : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void Walkable()
    {
        gameObject.SetActive(true);
    }
}
